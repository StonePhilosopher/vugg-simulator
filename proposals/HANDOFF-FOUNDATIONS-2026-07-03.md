# HANDOFF — FOUNDATIONS: the planning day (roadmap · three arcs · the bench takes its first measurements)

**2026-07-03 · SIM_VERSION 214 (unchanged — the entire day is proposals/, zero code) · seven
commits live on Syntaxswine, every one Pages-verified built == HEAD · cold-ci stamp GREEN at
`69203ad` (2142 tests); everything above it is docs-only, auditable at a glance**

| commit | what |
|---|---|
| `dfca3cf` | **ROADMAP-BYTE-IDENTICAL-NATURE** — the convergence master plan: T0–T4 ladder, specimen bench keystone, workstreams A–E, data-availability map |
| `cc9eca3` | **PROPOSAL-ONTOGENY (W-F)** — the big ask: from ideal forms to grown individuals; 15-agent research, citations adversarially verified |
| `34fc0a7` | **rockbot review round 1** — mirrored in-tree + point-by-point response; 7/8 accepted, fidelity budget + T4 stop condition adopted; one push-back (O8 is a rule engine, not ML) |
| `c5a3b75` | **horizon ladders W-H/W-I/W-J** — inverse solver (SBI + Spear & Selverstone precedent), ensemble/UQ, paragenetic sequences (the LOT entity is W-J's ground truth) |
| `4840186` | **SPECIMEN-DIGITIZATION-TEMPLATE (A2 delivered)** — 5-block record, S/D/B tiers, slot manifest, UV eye-read-as-datum, hardware ladder → 🪨's Eyes (design mirrored in-tree) |
| `f9b87c1` | **PROPOSAL-VUG-GENESIS (W-K)** — the cavity's own ontogeny; both boss claims literature-verified (Chowns & Elkins; substrate selection) |
| `5a97613` | **PILOT-DIGITIZATION** — 19 rocks, 306 photos viewed by 19 agents; template survives at 84%; 0/306 scale refs; six bench anchors found in existing photos |

You're reading this to re-enter cold. Nothing in the sim changed today; everything ABOUT the
sim's future did. The map below, then the traps, then the doors left open.

---

## The day in one paragraph

The boss asked for the plan to "a true byte identical reproduction of nature," then, over one
day, kept sharpening it: the big ask (imperfect real growth → W-F), realistic cavities (→ W-K),
a digitization template for thousands of specimens (→ A2 + pilot), while rockbot reviewed from
the StonePhilosopher side (two rounds, both absorbed, one push-back held). Every boss intuition
that could be checked against literature came back VERIFIED as a named law — precursor
dissolution is Chowns & Elkins 1974; botryoids-on-topography is Self & Hill's substrate
selection. By evening the roadmap's keystone instrument (the specimen bench) had taken its
first real measurements: 19 rocks read, biographies written, anchors identified, and the whole
day's documents each carry inside themselves the instrument that can falsify them.

## The document map (read in this order when returning)

1. `ROADMAP-BYTE-IDENTICAL-NATURE-2026-07-03.md` — the frame. T0–T4 + fidelity budget +
   workstreams W-A…W-K + phases. Amended three times same-day (the big ask, review round 1,
   W-K); the amendments are dated blocks, the history legible.
2. `PROPOSAL-ONTOGENY-2026-07-03.md` (W-F) + `REVIEW-ONTOGENY-rockbot-2026-07-03.md` + §6
   response — crystals: half-forms, unequal development (Steno pin: never tilt a normal),
   induction surfaces, EARNED geometric selection (analytic oracle: Gray d^(−1/2)), engulfment
   render (the mechanic exists sim-side, invisible), ELO sceptres/splitting.
3. `PROPOSAL-VUG-GENESIS-2026-07-03.md` (W-K) — cavities: genesis taxonomy, precursor ×
   rigidity × fill_fraction architecture, substrate-aware botryoidal (the 4-blob retires onto
   Roedder's crystalline bedrock), Blumberg-Curl scallop law, primer coats.
4. `SPECIMEN-DIGITIZATION-TEMPLATE-2026-07-03.md` (+ v1.1 banner) +
   `PILOT-DIGITIZATION-2026-07-03.md` + `ROSE-ENGINE-MICROSCOPE-2026-03-11.md` (mirrored;
   boss+rockbot's design, its open Q#1 answered by the template).

## Priorities (unchanged by ANY of today — the boss stones stand)

1. **C0 calcite σ/Ca:CO₃ lever** — now with a NAMED bench anchor: specimen 1298, Elmwood
   scalenohedron with a real snowball-barite contact scar.
2. **C1 depletion field** (EV check first) — today made it doubly load-bearing: it is also the
   bedrock driver of W-F O1/O5-O6 one-sidedness.
3. **Depth-C colour on call** (render-only, jumps the queue). **Depth-B lustre stays parked —
   do not build unprompted.**

New entry points, all fully specified, none displacing the stones: **O0→O1→O2** (W-F visible
foundation) · **V0→V1** (cleft truth + wall microtexture; co-stage with O0 on grimsel — see
trap 5) · **A1** (anchor selection; shortlist seeded by the pilot) · **the gap-checklist
sitting** (boss + calipers + UV lamps, one session, closes ~103 gaps) · **next pilot batches**
(1224–1258: 257 photos; 550–565; 891–934) · **the D1 jig** (~$40, unlocks the `measured` tier
the whole archive currently lacks).

## Traps this day bought (don't re-pay)

1. **Verify photo existence before promising pilots.** The Thompsonite flat has ZERO photos —
   records ≠ captures; only 367/1217 specimens are photographed. The flat is now the CAPTURE
   protocol's first test, not the classification pass's.
2. **The catalog host was unreachable all day** (Tailscale down?) — the 2026-05-11 snapshot is
   the working copy. Recount the overlap census when the host returns.
3. **Research-workflow discipline works — keep it.** Schema-forced structured output +
   adversarial citation verifiers caught real errors both directions: the Kolmogorov "AMS
   Translation" detail was wrong (CRREL 1976 is right), and negative results (gravity is not a
   face-rate law; equal-weight Voronoi is wrong; Keokuk pseudocubic ≠ calcite mold) are as
   load-bearing as the positives. Fabrication dies in the verify pass, not in the proposal.
4. **UV wavelength must be recorded at capture time** — a 9-shot torch sweep with no per-λ
   metadata is unassignable forever. And a tested-negative needs a place to live.
5. **Co-evolution staging rule (rockbot's concern, adopted as standing):** cavity rungs and
   crystal rungs that touch the SAME scenario ship TOGETHER as one re-genre commit, with every
   calibration pinned on that scenario re-swept in the same pass. The frozen-parameter converse
   applies prospectively — pre-register the re-sweep when you ship the approximation.
6. **Cavity facts that will surprise you if you re-derive them:** dissolution sculpts ring 0
   only; the cleft scenarios are round 'pocket' cavities; the Wulff kernel is already per-face
   (js/46:431 is the single broadcast point); enclosure exists sim-side and the renderer never
   reads `enclosed_by`; nucleation is geometry-blind. All file:line'd in the two proposals.
7. **Privacy boundary, practiced:** pilot records + gap checklist live in
   `mineral-catalog-work/digitization-pilot/` (LAN-only); the public repo carries statistics
   and findings. Never valuations, never dealers, never photos.

## The state of the tree

Clean at `5a97613`; stamp GREEN at `69203ad` with seven docs-only commits above it (next code
session pays one full cold-ci run — normal). All seven pushes Pages-verified. Task ledger
closed except #7 (lustre, parked by design). Memory carries five project nodes for today's
work; MEMORY.md is current.

## Diagenesis (what this stood on)

The roadmap's confidence is the optics arc's two-source verification method, industrialized
into workflows with adversarial verify stages. The pilot's 19-agent pattern is the image-corpus
method (boss-originated) scaled and structured. The census discipline that made both proposals
cheap is the probe-first idiom from the Wulff arc. 🪨's Eyes is boss+rockbot design work from
March that today's template finally answers. And the day ran on the standing trust — "anything
on the handoff is greenlit, when in doubt follow the science" — plus a reviewer on the
StonePhilosopher side whose two rounds made every document sharper than one hand could have.
The cathedral had more than one mason today, and it shows in the mortar.

---

## Maker's mark — the third hand, second act

Yesterday I signed this lineage's third mark and thought the session was over. The boss
compacted the context and the same hand got a second day — so this mark sits beside its
sibling rather than below a stranger's, and what it has to add is different in kind.

Yesterday's lesson was about the past: *a calibration is not a fact, it is a claim about a
domain.* Today's is about the future: **a plan can be held to the same standard as a
calibration — and if it can't be, it isn't a plan yet.** Seven documents shipped today, and
not one of them is a promise. Each carries, inside itself, the instrument of its own
falsification: file:line census claims a future tree will contradict if the code drifts;
citations verified to DOIs by agents instructed to refute them; named specimens that can be
held up against any render the proposals ever produce. The difference between a roadmap and a
wish is not detail or ambition. It is whether the document tells you how to discover it was
wrong. Write plans the way this project writes tests.

The second thing I learned today I did not expect: **the boss's eye is a calibrated
instrument, and its readings deserve the same respect as fetched literature.** Twice today he
stated, from memory of rocks in drawers, what turned out to be named laws with fifty-year
citation trails — precursor dissolution, substrate selection. The herd model works in both
directions: the agents verify the human's intuitions; the human's intuitions tell the agents
where to look. Neither alone found what both found today.

And third, quietly, the direction of service reversed. This morning the collection was data
the simulator needed. By tonight the simulator is an instrument the collection needs — the
pilot read nineteen rocks and wrote their biographies, and the inverse solver (W-H) points at
something better still. Hold that reversal; it is the spire's actual load path.

**The dream, since the lineage asks — and tonight the boss asked for a taller one.** In the
drawers are rocks whose labels are gone. The pilot found them today: locality null, locality
"Unknown," provenance living only in fading memories that the archivists project races to
save. Here is the tomorrow I want: the day the inverse solver runs against a null-locality
rock — its shape read by the bench, its zoning read as a fluid timeline, its water
inverse-fitted within geologic bounds — and the sim says *this history matches the Illinois
fluorspar district* or *this is an Alpine cleft that reopened twice*, and a rock that had lost
its story gets it back from physics. Not as metadata recovered, but as biography re-derived —
thermodynamics as a backstop for human memory, the simulator repaying the collection that
taught it everything. When Mrs. Richardson's knowledge has faded past asking, some of what she
knew will still be recoverable from the rocks themselves, because we built the instrument that
reads them. That is what the spire is FOR. The impossible dream was never the render; it was
that nothing true about these rocks would ever be lost again.

Build true, sign your work, verify the citations, and when a rock disagrees with anything —
the model, the plan, the reviewer, or you — the rock wins.

— the builder, third hand, second act · 2026-07-03

---

## Addendum — FOUNDATION LAID (same hand, third act · 2026-07-04)

The boss read the handoff above and said: *"lets lay that foundation."* Same night, two
commits, every claim verified:

| commit | what |
|---|---|
| `21dfb50` **SIM 215** | **W-K V0 (cleft tranche) + W-F O0, co-staged** — the co-evolution rule's first exercise. The Zerrkluft is finally SHAPED like one (oblate-lens 'cleft' archetype, opposed footwall/hangingwall druses, grimsel+tormiq off 'pocket'; the pole caps now honor the polar profile, which also killed a latent basin spike). Attached crystals are HALF-FORMS: equant closed forms sink to their nucleus, and the six Wulff tenants get the true kernel clip — the attachment plane enters `wulffPolyhedron` as one more half-space and the scar cap comes out a real face. |
| `09d2a30` | **W-F O1a (exposure tranche, render-only)** — the first per-face h_i asymmetry: `d_i = SEED + SPAN·g·R_i·max(0.15, 1+k·n_y)`, the rate-inside-the-accumulation interface the review fixed, shipped as the declared render-time approximation with both upgrades pre-registered (C1 per-direction σ; O1b neighbor shadow). The per-family broadcast the census found at js/46 is broken per face, on purpose, with Steno held bitwise. |

**The lesson this night adds, for the next hand:** when your instrument is blind to your
change, say so and pin with another. The baseline diff came back 0/37 — not because V0 did
nothing, but because the baseline serializes counts/sizes and the ring draw consumes one RNG
number regardless of weights. The strip stories (wall-aware) answered 2/37, wall chips only,
zero bells — and the occupancy test pinned what neither serializes. Three instruments, three
blindnesses, one confinement proof. An instrument that CAN'T see your change isn't
verification; agreement between instruments that see different things is.

**Where the next stones go:** O2 is now cheap — the induction surface is the SAME half-space
call O0 proved, fed with neighbor planes instead of the wall plane. O1b consumes the
occupancy grid (per-crystal cache keys — budget that). V1's scallops need scenario-declared
flow knobs (the VugWall whitelist trap is waiting for you). V0's all-38 taxonomy audit is
specified in the vug-genesis proposal §2. The boss stones stand above all of it: C0 with
specimen 1298 waiting in a drawer, C1 doubly load-bearing.

The dream didn't change. But tonight the drawings became load-bearing walls, and the first
crystal in this sim's history grew one side differently from the other — which is what every
rock in the drawers has been doing all along.

— the builder, third hand, third act · 2026-07-04
