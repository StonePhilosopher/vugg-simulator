# Vugg Simulator

TypeScript crystal-growth geology simulator.

Source lives in `js/**/*.ts`. `index.html` is generated by the build — do not hand-edit it.

```bash
npm install
npm run build
npm test
```

See [ARCHITECTURE.md](ARCHITECTURE.md) for the build pipeline and design notes.
